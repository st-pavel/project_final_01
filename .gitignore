# ==========================================
# 1. СИСТЕМНЫЕ ФАЙЛЫ И IDE
# ==========================================
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*Zone.Identifier

# ==========================================
# 2. PYTHON И ОКРУЖЕНИЕ
# ==========================================
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
ENV/

# ==========================================
# 3. JUPYTER NOTEBOOKS
# ==========================================
.ipynb_checkpoints/
*/.ipynb_checkpoints/
# Папка, которую создает CatBoost при обучении
catboost_info/

# ==========================================
# 4. ДАННЫЕ (DATA)
# ==========================================
# Игнорируем всё в папке data...
data/*
# ...кроме самого архива (он нужен для работы)
!data/data.csv.zip
# ...и файла-маркера (если есть)
!data/.gitkeep

# Игнорируем распакованные данные в корне (если случайно распаковали)
data.csv

# ==========================================
# 5. МОДЕЛИ (MODELS)
# ==========================================
# Игнорируем тяжелые веса моделей
models/*
*.pkl
*.pickle
*.h5
*.pt
*.pth
*.onnx
histgradientboosting.pkl
stacking_rf_catboost_ridge.pkl
stacking_rf_histgb_ridge.pkl
stacking_triple_xgboost.pkl

# ВАЖНО: Разрешаем (White-list) конкретную модель для деплоя
# (Убедитесь, что она < 100 МБ)
!deployment/tuned_xgboost.pkl
!models/tuned_xgboost.pkl

# ==========================================
# 6. ЛОГИ И ВРЕМЕННЫЕ ФАЙЛЫ
# ==========================================
# Игнорируем все логи...
logs/*
# ...кроме финального отчета с результатами
!logs/results_release.csv

# Временные файлы LibreOffice/Excel (когда файл открыт)
.~lock.*
tmp/
cache/

# ==========================================
# 7. ОТЧЕТЫ И ГРАФИКИ
# ==========================================
# Игнорируем HTML отчеты (они тяжелые)
*.html
# Но оставляем ZIP-архив с отчетом
!project_final_html.zip

# ==========================================
# 8. РАЗНОЕ (Глобальный игнор мусора)
# ==========================================
# Игнорируем CSV везде, чтобы не засорять репо...
*.csv
# ...НО разрешаем нужные нам конфигурационные CSV
!deployment/model_input_examples.csv
!logs/results_release.csv
!assets/*.csv

# PDF оставляем (ТЗ)
!brief_prediction.pdf

# ==========================================
# 9. КОРНЕВЫЕ ФАЙЛЫ (Явное разрешение)
# ==========================================
# На всякий случай явно разрешаем ключевые файлы
!project_final.ipynb
!requirements.txt
!runtime.txt
!README.md
!LICENSE