# 🏠 Real Estate Price Prediction Service

![Python](https://img.shields.io/badge/Python-3.12-blue)
![Scikit-Learn](https://img.shields.io/badge/Sklearn-Pipeline-orange)
![XGBoost](https://img.shields.io/badge/XGBoost-GPU-green)
![Optuna](https://img.shields.io/badge/Optuna-Tuning-blueviolet)

## 📋 Описание проекта

Проект выполнен в рамках итоговой аттестации первого года обучения по специальности **Data Science**.

**Бизнес-задача:**
Крупное агентство недвижимости столкнулось с проблемой: риелторы тратят слишком много времени на оценку объектов, а качество ручной оценки уступает конкурентам. Поставлена задача - разработать сервис для предсказания стоимости домов на основе истории предложений. Точная модель оценки позволит ускорить работу и повысить качество сделок.

**Цель:** Разработать сервис (ML-модель) для автоматического предсказания стоимости домов на основе истории предложений, что позволит ускорить работу риелторов и повысить точность ценообразования.

---

## 💾 Данные

В работе используется датасет `data.csv` (~377k записей) с историей продаж недвижимости в США.
Данные "грязные" и требуют сложной предобработки.

**Основные признаки:**
*   `status`: Статус продажи (активно, продано, новое строительство).
*   `propertyType`: Тип недвижимости (дом на одну семью, таунхаус и т.д.).
*   `street`, `city`, `state`, `zipcode`: Географические данные.
*   `baths`, `beds`: Количество ванных и спален.
*   `sqft`: Площадь дома.
*   **Сложные признаки (JSON):** `homeFacts` (год постройки, отопление, парковка) и `schools` (рейтинг школ и расстояние до них).

---

## 🛠 Технологический стек

*   **Язык:** Python 3.12
*   **Анализ данных:** Pandas, NumPy, D-Tale
*   **Визуализация:** Plotly, Seaborn, Matplotlib
*   **ML библиотеки:** Scikit-learn, XGBoost, CatBoost
*   **Тюнинг и инструменты:** Optuna, Category Encoders (Target Encoding)
*   **Оборудование:** Проект оптимизирован для работы на **GPU (NVIDIA CUDA)** для ускорения обучения ансамблей.

---

## 🚀 Этапы работы

### 1. Разведочный анализ (EDA) и Очистка
*   Парсинг вложенных JSON-структур (`homeFacts`, `schools`) для извлечения полезных признаков.
*   Анализ распределений с помощью **Plotly** и **D-Tale**.
*   Удаление аномальных выбросов (дома площадью > 7 млн кв. футов, нереалистичные цены).

### 2. Feature Engineering
*   **География:** Использование координат (`latitude`, `longitude`), а также использование **Target Encoding** для почтового индекса (`zipcode`), что дало прирост метрики $R^2$ на 17%.
*   Создание новых признаков: возраст дома, расстояние до школ, средний рейтинг района.

### 3. Методология и Пайплайны
*   **Устранение Data Leakage:** В ходе анализа референсного решения была выявлена утечка данных (заполнение пропусков до сплита). Весь процесс обработки был переписан с использованием `sklearn.pipeline.Pipeline`, что гарантирует честную валидацию.
*   Реализован полный цикл: `Imputer` -> `Scaler` -> `Encoder` -> `Model`.

### 4. Моделирование и ML
Были протестированы три подхода:
1.  **Baseline:** Одиночная модель GradientBoosting "из коробки".
2.  **Optimization:** Подбор гиперпараметров для XGBoost с помощью **Optuna** (20 итераций).
3.  **Advanced Stacking:** Построение ансамбля (StackingRegressor) из глубоких и поверхностных деревьев XGBoost с использованием **GPU-ускорения**.

---

## 📊 Результаты

Финальное сравнение моделей на отложенной тестовой выборке:

| Модель | R2 Score | MAE (Ошибка в $) | Комментарий |
|:--- | :--- | :--- | :--- |
| **Baseline Pipeline** (GradientBoosting) | 0.80 | $116,250 | Надежное решение на CPU |
| **Tuned XGBoost** (Optuna) | **0.83** | **$101,759** | **Рекомендовано к внедрению (Баланс скорости/точности)** |
| **GPU Stacking** (Double XGBoost) | **0.84** | **$99,82** | Лучшая точность, требует GPU |

**Бизнес-вывод:**
Разработанная модель ошибается в среднем на **$102k** (для Tuned версии), что составляет около **15%** от средней стоимости объекта. Модель способна автоматически ранжировать предложения и выявлять недооцененные/переоцененные объекты.

---

## 📂 Структура репозитория

```text
├── data/                  # Исходные данные (в архиве)
└── project_final.ipynb # Основной ноутбук с решением
├── models/                # Сохраненные модели (pkl)
├── logs/                  # Логи обучения (txt)
├── requirements.txt       # Список зависимостей
├── brief_prediction.pdf   # Описание бизнес-задачи
├── project_final_html.zip # Aрхив, содержащий HTML-файл с результатами и логами
└── README.md              # Описание проекта
```

---
🔧 Установка и запуск

Клонируйте репозиторий:
``` Bash
git clone https://github.com/st-pavel/project_final_01.git
```

Установите зависимости:
# Примечание: Если вы используете GPU (NVIDIA) то PyTorch лучше ставить отдельной командой для GPU - ДО установки всех библиотек.
``` Bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```
и потом 
``` Bash
pip install -r requirements.txt
```

Примечание: Для работы GPU-стекинга требуются установленные драйверы CUDA и torch.
Запустите ноутбук notebooks/project_final.ipynb.

## Результаты и логи
Так как логи обучения занимают более 100 МБ, полный отчет о выполнении кода 
(с графиками и метриками) упакован в архив:

📂 **[project_final_html.zip](./project_final_html.zip)**

> **Инструкция для проверки:** > Пожалуйста, скачайте этот архив, распакуйте и откройте HTML-файл в браузере. 
> Там сохранен весь вывод ячеек, включая интерактивные графики и ход обучения моделей.