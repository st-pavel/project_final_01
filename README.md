# 🏠 Real Estate Price Prediction Service

![Python](https://img.shields.io/badge/Python-3.12-blue)
![Scikit-Learn](https://img.shields.io/badge/Sklearn-Pipeline-orange)
![XGBoost](https://img.shields.io/badge/XGBoost-GPU-green)
![Optuna](https://img.shields.io/badge/Optuna-Tuning-blueviolet)
![Status](https://img.shields.io/badge/Status-Completed-success)

## 📋 Описание проекта

Проект выполнен в рамках итоговой аттестации первого года обучения на курсе SkillFactory по специальности **Data Science**.

### 🎯 Бизнес-задача
Крупное агентство недвижимости столкнулось с проблемой: риелторы тратят слишком много времени на оценку объектов, а качество ручной оценки уступают конкурентам (AVM-системам).

**Цель:** Разработать ML-сервис для автоматического предсказания рыночной стоимости жилья. Это позволит:
1.  **Ускорить работу** риелторов (мгновенная оценка).
2.  **Повысить качество сделок** (выявление недооцененных лотов).
3.  **Снизить риски** человеческого фактора при ценообразовании.

---

## 🌟 Ключевые особенности решения
*   **No Data Leakage:** Реализован строгий пайплайн (Pipeline), исключающий утечку данных при предобработке.
*   **Advanced Feature Engineering:** Использование **Target Encoding** для геоданных дало прирост точности на **17%**.
*   **High Performance:** Использование **GPU (CUDA)** для обучения сложных ансамблей (Stacking).
*   **Automated Tuning:** Подбор гиперпараметров через **Optuna**.

---

## 💾 Данные

В работе используется датасет `data.csv` (~377k записей) с историей продаж недвижимости в США.

**Основные группы признаков:**
*   **Характеристики:** `sqft` (площадь), `lotsize` (участок), `beds`, `baths`, `stories`.
*   **Локация:** `zipcode`, `city`, `state`, `latitude`, `longitude`.
*   **Сложные структуры (JSON):** `homeFacts`, `schools` (рейтинг и расстояние).
*   **Целевая переменная:** `target` (стоимость сделки).

---

## 🚀 Этапы работы

1.  **Разведочный анализ (EDA) и Очистка:**
    *   Парсинг JSON-структур.
    *   Удаление аномальных выбросов (площадь > 7 млн кв. футов, ошибки ввода).
    *   Интерактивный анализ через **D-Tale**.

2.  **Feature Engineering:**
    *   Восстановление координат через библиотеку `pgeocode`.
    *   Применение **Target Encoding** для высококардинального признака `zipcode`.

3.  **Моделирование и ML:**
    *   **Baseline:** HistGradientBoosting (Sklearn).
    *   **Optimization:** XGBoost + Optuna (20 trials).
    *   **Advanced:** StackingRegressor (Triple XGBoost Deep+Midddle+Shallow) на GPU.

---

## 📊 Результаты

Финальное сравнение моделей на отложенной тестовой выборке (Test):

| Модель | R2 Score | MAE ($) | Статус |
|:--- | :---: | :---: | :--- |
| **Baseline Pipeline** (HistGradientBoosting) | 0.80 | $116,218 | Надежное CPU решение |
| **Tuned XGBoost** (Optuna) | **0.82** | **$106,199** | **Рекомендовано к внедрению (Production)** |
| **GPU Stacking** (Triple XGBoost) | **0.84** | **$96,223** | Максимальная точность (Research) |

> **Бизнес-вывод:** Финальная модель ошибается в среднем на **~15%** от стоимости объекта. Это позволяет использовать её для автоматического скоринга новых объявлений и приоритизации работы агентов.

---

## 📂 Структура репозитория

```text
├── brief_prediction.pdf   # ТЗ / Описание задачи
├── data/                  # Исходные данные (data.csv.zip)
├── deployment/            # 🐳 Docker, Flask API и клиент
│   ├── app.py
│   ├── client.py
│   ├── Dockerfile
│   ├── google_apps_script.js
│   ├── model_input_examples.csv
│   ├── requirements.txt
│   └── tuned_xgboost.pkl
├── logs/                  # История экспериментов и метрик (.log, .csv)
├── models/                # Сериализованные модели (.pkl)
├── project_final.ipynb    # ⭐️ Основной ноутбук с решением
├── project_final_html.zip # HTML-отчет (вывод всех ячеек и графиков)
├── requirements.txt       # Список зависимостей
├── runtime.txt            # Версия Python (3.12.3)
├── LICENSE                # Лицензия MIT
└── README.md              # Документация проекта
```

---

## 🔧 Установка и запуск

Проект разработан на **Python 3.12**.

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/st-pavel/project_final_01.git
   cd project_final_01
   ```

2. **(Опционально) Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Mac/Linux
   venv\Scripts\activate     # Windows
   ```

3. **Установите зависимости:**
   
   *Если вы планируете использовать GPU (NVIDIA), сначала установите PyTorch с поддержкой CUDA (команда зависит от вашей ОС и версии драйвера, см. [pytorch.org](https://pytorch.org/)):*
   ```bash
   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
   ```
   
   *Затем установите остальные библиотеки:*
   ```bash
   pip install -r requirements.txt
   ```

4. **Запуск:**
   ```bash
   jupyter notebook project_final.ipynb
   ```

---

## 💡 Примечания

### Воспроизводимость (Reproducibility)
В проекте зафиксированы `random_state`. Однако из-за особенностей GPU-вычислений и многопоточности Intel sklearnex метрики могут незначительно колебаться (в пределах 0.001).
Для проверки результатов без перезапуска вычислений используйте **HTML-отчет**:
📂 **[Скачать отчет (project_final_html.zip)](./project_final_html.zip)**

### D-Tale
Для запуска интерактивного отчета D-Tale раскомментируйте строку `GENERATE_DTALE = True` в начале ноутбука. Отчет будет доступен по локальной ссылке (указывается в логах).

---

## 🐳 Деплой и запуск (Docker)

В проекте реализован микросервис на **Flask**, который загружает обученную модель и предоставляет API для оценки стоимости недвижимости. Сервис упакован в **Docker-контейнер**, что гарантирует воспроизводимость на любой машине.

### Структура модуля деплоя:
*   `deployment/app.py` — Flask-сервер (REST API).
*   `deployment/client.py` — Клиентский скрипт для проверки.
*   `deployment/Dockerfile` — Инструкция для сборки образа.

### Инструкция по запуску:

1. **Сборка образа:**
   Находясь в корне проекта, выполните:
   ```bash
   cd deployment
   docker build -t estate-price-predictor .
   ```
2. Запуск контейнера:
   ```bash
   docker run -d -p 5000:5000 --name estate-price-predictor estate-price-predictor
   ```
   Сервер запустится на порту 5000.
3. Проверка работы (Клиент):
   В другом терминале запустите скрипт проверки:

   ```bash
   python client.py
   ``` 
  
  Скрипт отправит тестовый запрос с параметрами дома и выведет предсказанную цену.

Интеграция
API принимает JSON-запросы по адресу http://localhost:5000/predict и может быть интегрирован с любым фронтендом, мобильным приложением или Google Таблицами (через ngrok).

### 📊 Клиент в Google Таблицах

Для удобства демонстрации реализован клиент в Google Sheets с использованием Apps Script.

**Вариант 1. Использовать шаблон (Рекомендуется)**
1. [Создать копию таблицы с готовым скриптом](https://docs.google.com/spreadsheets/d/1jng87J-kGyoWfdbnsORA5hflVY-XJ0ervnCH3abGj2s/edit?gid=1545271700#gid=1545271700/copy)
2. В меню таблицы выбрать: **Расширения > Apps Script**.
3. В строке `var url = ...` вставить ваш актуальный адрес ngrok (например, `https://xxxx.ngrok-free.app/predict`).
⚠️ Внимание: URL от ngrok может меняться. Скопируйте Ваш актуальный URL и вставьте его в скрипт Google Таблицы перед использованием!
4. Сохранить и обновить страницу таблицы.
5. Меню **"🏡 ML Оценка"** появится автоматически.
⚠️ Внимание: Если после обновления страницы Google Sheets - меню '🏡 ML Оценка' не появилось, откройте Расширения > Apps Script и запустите функцию onOpen вручную.

**Вариант 2. Ручная установка**
1. Создайте новую таблицу и импортируйте заголовки из `deployment/model_input_examples.csv`.
2. Скопируйте код из файла [`deployment/google_apps_script.js`](./deployment/google_apps_script.js).
3. Вставьте его в редактор скриптов таблицы.

---

## 👨‍💻 Автор
Студент курса Data Science (SkillFactory).
